def power_set(nums):
    res = [[]]
    for num in nums:
        res += [curr + [num] for curr in res]  # ✅ Add all combinations
    return res

print(power_set([1, 2, 3]))
# ✅ [[], [1], [2], [1,2], [3], [1,3], [2,3], [1,2,3]]
